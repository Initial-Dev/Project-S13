import {FunctionComponent} from "react";

type Props = {
    className?: string
}
const dragDrop: FunctionComponent<Props> = ({className}) => {
    return (
        <div>
            dragdrop
        </div>
    )
}

export default dragDrop;
